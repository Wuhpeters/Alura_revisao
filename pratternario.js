/*const peso = 100

const result = (peso <= 100 ? "Tudo normal" : "Acima do peso")

console.log(result)*/

const velocidade = 110

const res = (velocidade > 80 ? "Acima do limite permitido" : "Velocidade permitida")

console.log(res)